#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pb push_back
#define all(x) x.begin(), x.end()
#define endl '\n'
int trap(vector<int>& height) {
    int left = 0;
    int right = height.size() - 1;
    while (left < right) {
    }
    return 0;
}
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    return 0;
}
