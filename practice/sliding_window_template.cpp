#include <bits/stdc++.h>
using namespace std;

// Implement sliding window to find maximum sum subarray of size k
// Input: Array of integers, window size k
// Output: Maximum sum of any subarray of size k
int max_subarray_sum(const vector<int>& nums, int k) {
    // TODO: Implement sliding window algorithm
    // 1. Calculate sum of first k elements
    // 2. Initialize max sum as this value
    // 3. Slide the window by removing one element from left and adding one from right
    // 4. Update max sum if the current window sum is greater
    // 5. Return the maximum sum found
    
    return 0; // Replace with your implementation
}