#include <bits/stdc++.h>
using namespace std;

// Implement topological sort on a directed acyclic graph (DAG)
// Input: Graph as adjacency list
// Output: Vector containing nodes in topological order
vector<int> topological_sort(const vector<vector<int>>& graph) {
    // TODO: Implement topological sort
    // 1. Calculate in-degree for each node
    // 2. Enqueue nodes with in-degree 0
    // 3. While queue is not empty:
    //    a. Dequeue a node and add it to result
    //    b. Reduce in-degree of its neighbors
    //    c. Enqueue neighbors with in-degree 0
    // 4. Return the topological order
    
    return {}; // Replace with your implementation
}