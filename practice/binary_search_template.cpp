#include <bits/stdc++.h>
using namespace std;

// Implement binary search on a sorted array
// Input: Sorted array, target value
// Output: Index of target if found, -1 otherwise
int binary_search(const vector<int>& nums, int target) {
    // TODO: Implement binary search
    // 1. Initialize left and right pointers
    // 2. While left <= right:
    //    a. Calculate mid point
    //    b. If target found at mid, return mid
    //    c. If target > mid value, search right half
    //    d. If target < mid value, search left half
    // 3. Return -1 if target not found
    
    return -1; // Replace with your implementation
}