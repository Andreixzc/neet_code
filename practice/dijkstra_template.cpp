#include <bits/stdc++.h>
using namespace std;

// Implement Dijkstra's algorithm for finding shortest paths
// Input: Graph as adjacency list of pairs (neighbor, weight), source node
// Output: Vector of shortest distances from source to all nodes
vector<int> dijkstra(const vector<vector<pair<int, int>>>& graph, int source) {
    // TODO: Implement Dijkstra's algorithm
    // 1. Initialize distances array with infinity for all nodes except source
    // 2. Use a priority queue (min heap) to process nodes in order of distance
    // 3. For each node, relax all outgoing edges
    // 4. Return the array of shortest distances
    
    return {}; // Replace with your implementation
}